[mypy]
python_version = 3.10
ignore_missing_imports = True
no_implicit_optional = True
warn_unused_configs = True
strict_optional = True
# Keep it simple; no verbose regex (that caused your parse error)
exclude = ^(models_cache/|artifacts/)$

# Silence noisy modules for now; we’ll tighten later
[mypy-sro.prover.s2_frontier]
ignore_errors = True

[mypy-sro.prover.s5_bounded_search]
ignore_errors = True
